package org.usfirst.frc.team1710.robot.commandGroups;

import edu.wpi.first.wpilibj.command.CommandGroup;

/**
 *
 */
public class AutoChallenge extends CommandGroup {

    public AutoChallenge() {
    	//use addSequential( new DriveToPosition(distance (inches), heading (0 degrees), power (decimal between 0 and 1)); to move forward. 
    	//use addSequential( new RotateToAngle(degrees)); to turn
    }
}
